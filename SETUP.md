Enter# جولة V4 — ربط قاعدة البيانات الحقيقية

هذا التحديث يحوّل الموقع من "معاينة محلية" (localStorage) إلى موقع حقيقي:
أي خبر تنشرينه من `/admin.html` يظهر مباشرة لكل الزوار من أي جهاز.

اتبعي الخطوات بالترتيب — كل خطوة مرة واحدة فقط.

---

## 1) ارفعي كل الملفات

ارفعي كل ملفات هذا المجلد إلى نفس مكان المشروع في GitHub (استبدال الملفات القديمة بالكامل)،
بما في ذلك **مجلد `netlify/functions/` الجديد** و`migration.sql` و`package.json` و`netlify.toml` المحدَّثين.

## 2) شغّلي تحديث قاعدة البيانات (مرة واحدة)

1. من لوحة Netlify: **Database → production → SQL console**
2. افتحي ملف `migration.sql` المرفق، وانسخي **كل محتواه**، والصقيه في الـ SQL console، ثم نفّذيه (Run / Execute)
3. تأكدي بعدها إن جدول `articles` صار فيه 3 أخبار افتراضية (نفس التي كانت تظهر بالمعاينة)، وجدول `reporters` فيه 4 مراسلين

هذا الملف آمن للتنفيذ أكثر من مرة بالغلط — لن يكرر البيانات ولن يحذف شيئًا موجودًا.

## 3) أضيفي متغيرات البيئة (مرة واحدة)

من لوحة Netlify: **Site configuration → Environment variables → Add a variable**، أضيفي:

| المتغير | القيمة | ملاحظة |
|---|---|---|
| `ADMIN_PASSWORD` | كلمة مرور من اختيارك | هذه كلمة مرور الدخول للوحة التحكم الجديدة — اختاريها قوية ولا تشاركيها |
| `TOKEN_SECRET` | نص عشوائي طويل (مثلاً 32 حرفًا) | يُستخدم داخليًا فقط لتوقيع جلسة الدخول؛ لو تركتيه فارغًا سيُستخدم `ADMIN_PASSWORD` بدلاً منه، لكن يُفضّل ضبطه لزيادة الأمان |

**لا تحتاجين إضافة** `NETLIFY_DATABASE_URL` — Netlify يوفرها تلقائيًا لأن قاعدة البيانات مربوطة بالمشروع أصلاً (شفناها بلوحة Database سابقًا).

## 4) أعيدي النشر (Redeploy)

بعد رفع الملفات وضبط المتغيرات، من تبويب **Deploys** اضغطي **Trigger deploy → Deploy site** لإعادة البناء بالإعدادات الجديدة.

## 5) جرّبي

1. افتحي `/admin.html` وسجّلي الدخول بكلمة المرور اللي ضبطيها في `ADMIN_PASSWORD`
2. أنشئي خبرًا تجريبيًا وانشريه
3. افتحي الموقع الرئيسي `/` (من جهاز آخر أو متصفح آخر لتتأكدي) — يجب أن يظهر الخبر فورًا

---

## ملاحظات مهمة

- **الأمان:** التوثيق الآن حقيقي (كلمة المرور محفوظة في إعدادات Netlify وليس داخل الكود)، لكنه ما زال بسيطًا (كلمة مرور مشتركة واحدة، بدون حسابات متعددة أو صلاحيات). يكفي لفريق صغير موثوق، لكنه ليس بديلاً عن نظام مستخدمين حقيقي (Netlify Identity أو مشابه) لو كبر الفريق مستقبلاً.
- **رفع الصور:** حاليًا رفع صورة من الجهاز يحوّلها إلى نص طويل (Base64) ويُخزَّن كما هو. هذا يعمل للصور الصغيرة لكنه غير مثالي وقد يفشل مع صور كبيرة الحجم أو يُبطئ الصفحة. الأفضل حاليًا: استخدام رابط صورة خارجي (مثلاً من موقع استضافة صور) بدل الرفع المباشر، إلى حين إضافة نظام رفع حقيقي (Netlify Blobs أو مشابه) في مرحلة قادمة.
- **قسم "البرامج الاجتماعية"** على الموقع لسه محتوى ثابت (لا توجد لوحة تحكم له بعد) — نفس الوضع السابق، لم يتغيّر في هذا التحديث.
- **جدول `audit_log`** تم إنشاؤه في قاعدة البيانات تحسبًا لمرحلة قادمة (تتبّع من عدّل ماذا ومتى)، لكنه غير مستخدم من الكود حاليًا.
